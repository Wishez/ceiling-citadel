# -*- coding: utf-8 -*-
# from django.conf.urls import url
# from .views import *
# from .api.views import *

# urlpatterns = [
#     url('^api/product/$', CreateProductView.as_view(), name='ordered_product'),
#     url('^api/product/(?P<uuid>[-\w]+)/$', ProductView.as_view(), name='ordered_product'),
    # url('^api/add/$', add, name='add_ordered_product'),
    # url('^api/remove/$', remove, name='remove_ordered_product'),
# ]